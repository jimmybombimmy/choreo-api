# from sqlmodel import Session, select

# from app.core.db import engine

# from app.models.task_types import TaskTypes
# --create and import all models for creation--


# --seed-db-with-test-data--
#   - all fields must be named 'TEST'


# --finish by printing output of db seed--
# def select_task_types():
#     with Session(engine) as session:
#         statement = select(TaskTypes)
#         results = session.exec(statement)
#         for blah in results:
#             print(blah)

# select_task_types()
